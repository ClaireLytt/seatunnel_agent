services:
  agent:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./examples:/app/examples
      - agent-history:/root/.seatunnel-agent/chat_history
      - agent-configs:/app/configs

volumes:
  agent-history:
  agent-configs:
